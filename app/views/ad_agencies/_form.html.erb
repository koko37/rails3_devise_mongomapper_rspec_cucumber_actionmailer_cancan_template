<%= form_for(@ad_agency) do |f| %>
  <% if @ad_agency.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ad_agency.errors.count, "error") %> prohibited this ad_agency from being saved:</h2>

      <ul>
      <% @ad_agency.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :photo_url %><br />
    <%= f.text_area :photo_url %>
  </div>
  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
