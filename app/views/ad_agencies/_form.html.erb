<%= form_for @ad_agency, :html => { :class => 'form-horizontal' } do |f| %>
  <% if @ad_agency.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ad_agency.errors.count, "error") %> prohibited this ad_agency from being saved:</h2>

      <ul>
      <% @ad_agency.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :name, :class => 'control-label' %><br />
    <div class="controls">
        <%= f.text_field :name %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :description, :class => 'control-label' %><br />
    <div class="controls">
    <%= f.text_area :description,:rows=>5, :cols => 40 %>
    </div>
  </div>

  <div class="control-group">
    <%= f.label :photo_url, :class => 'control-label' %><br />
    <div class="controls">
        <%= f.text_area :photo_url,:rows=>5, :cols => 40 %>
    </div>
  </div>

  <div class="control-group">
    <%= f.label :email, :class => 'control-label' %><br />
    <div class="controls">
        <%= f.text_field :email %>
    </div>
  </div>
  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
  </div>
<% end %>
